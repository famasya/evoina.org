---
import { Check, Database, Users } from "@lucide/astro";
import { getCollection, type CollectionEntry } from "astro:content";

// Get all archive posts and sort by publication date (newest first)
const allPosts = await getCollection("archives");
const sortedPosts = allPosts.sort(
  (a: CollectionEntry<"archives">, b: CollectionEntry<"archives">) =>
    b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);

// Get the latest 5 posts for the news section
const latestNews = sortedPosts.slice(0, 5);
---

<div class="m-16">
  <div class="flex flex-row items-center justify-center gap-8 mb-8">
    <h2 class="font-serif text-3xl mb-4 bg-eggshell-500 px-4 py-1">
      Latest News & Updates
    </h2>
  </div>

  <!-- News Grid -->
  <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
    {
      latestNews.map((post, index) => (
        <article class="bg-gradient-to-b from-eggshell-600 to-eggshell-500 border border-eggshell-400 hover:border-eggshell-300 rounded-xl p-6 transition-all duration-300 hover:shadow-sm">
          <div class="flex flex-col h-full">
            <div class="flex items-center justify-between mb-3">
              <span class="inline-block bg-brunswick_green text-white text-xs px-3 py-1 rounded-full font-medium">
                {post.data.category}
              </span>
              <time class="text-sm text-dim_gray-400 font-medium">
                {post.data.pubDate.toLocaleDateString("en-US", {
                  year: "numeric",
                  month: "long",
                  day: "numeric",
                })}
              </time>
            </div>

            <h3 class="font-serif text-xl mb-3 text-night leading-tight">
              {post.data.title}
            </h3>

            <p class="text-dim_gray-400 text-base leading-relaxed flex-grow mb-4">
              {post.data.description}
            </p>

            <a
              href={`/post/${post.slug}`}
              class="inline-flex items-center text-brunswick_green hover:text-brunswick_green-600 font-medium text-sm transition-colors duration-200"
            >
              Read more
              <svg
                class="w-4 h-4 ml-1 transition-transform duration-200 group-hover:translate-x-1"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M9 5l7 7-7 7"
                />
              </svg>
            </a>
          </div>
        </article>
      ))
    }
  </div>

  <!-- Read All News Link -->
  <div class="text-center mb-12">
    <a
      href="/archives"
      class="inline-flex items-center px-6 py-3 bg-eggshell-400 hover:bg-eggshell-300 text-night rounded-lg font-medium transition-colors duration-200 shadow-sm hover:shadow-sm"
    >
      Read All News & Updates
      <svg
        class="w-5 h-5 ml-2"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
      </svg>
    </a>
  </div>

  <!-- Call-to-Action Section -->
  <div
    class="bg-gradient-to-r from-brunswick_green to-brunswick_green-600 rounded-2xl p-8 md:p-12 text-white"
  >
    <div class="max-w-4xl mx-auto text-center">
      <h3 class="font-serif text-2xl md:text-3xl mb-4">
        Join Our Community of Evolutionary Researchers
      </h3>
      <p class="text-lg md:text-xl mb-8 text-eggshell-900 leading-relaxed">
        Be part of Indonesia's premier evolutionary biology network. Connect
        with researchers, access exclusive resources, and contribute to
        groundbreaking discoveries.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <a
          href="https://tinyurl.com/dbmember"
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center px-8 py-4 bg-hunyadi_yellow-500 hover:bg-hunyadi_yellow-400 text-night rounded-lg font-semibold text-lg transition-colors duration-200 shadow-lg"
        >
          <Users class="w-6 h-6 mr-3" />
          Register as Member
        </a>

        <span class="text-eggshell-900 font-medium">or</span>

        <a
          href="https://tinyurl.com/dbspesies"
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center px-8 py-4 bg-transparent border-2 border-eggshell-800 hover:bg-eggshell-800 hover:text-brunswick_green text-eggshell-900 rounded-lg font-semibold text-lg transition-colors duration-200 shadow-lg"
        >
          <Database class="w-6 h-6 mr-3" />
          Contribute to DBSpesies
        </a>
      </div>

      <div class="mt-8 grid grid-cols-1 md:grid-cols-2 gap-6 text-left">
        <div class="bg-brunswick_green-700/30 rounded-lg p-6">
          <h4 class="font-semibold text-lg mb-2 text-hunyadi_yellow-800">
            Member Benefits
          </h4>
          <ul class="text-eggshell-900 space-y-2">
            <li class="flex items-center">
              <Check class="w-4 h-4 mr-1" />
              Access to exclusive research resources
            </li>
            <li class="flex items-center">
              <Check class="w-4 h-4 mr-1" />
              Priority registration for conferences
            </li>
            <li class="flex items-center">
              <Check class="w-4 h-4 mr-1" />
              Networking opportunities
            </li>
          </ul>
        </div>

        <div class="bg-brunswick_green-700/30 rounded-lg p-6">
          <h4 class="font-semibold text-lg mb-2 text-hunyadi_yellow-800">
            DBSpesies Project
          </h4>
          <ul class="text-eggshell-900 space-y-2">
            <li class="flex items-center">
              <Check class="w-4 h-4 mr-1" />
              Contribute to biodiversity database
            </li>
            <li class="flex items-center">
              <Check class="w-4 h-4 mr-1" />
              Share your research data
            </li>
            <li class="flex items-center">
              <Check class="w-4 h-4 mr-1" />
              Collaborate with researchers
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  /* Mobile optimizations */
  @media (max-width: 640px) {
    .grid {
      grid-template-columns: 1fr;
    }

    article {
      padding: 1.25rem;
    }

    h3 {
      font-size: 1.125rem;
      line-height: 1.4;
    }

    .bg-gradient-to-r {
      padding: 2rem 1.5rem;
    }

    .flex-col.sm\\:flex-row {
      gap: 1rem;
    }

    .inline-flex {
      width: 100%;
      justify-content: center;
    }

    @media (min-width: 640px) {
      .inline-flex {
        width: auto;
      }
    }
  }

  /* Focus states for accessibility */
  a:focus {
    outline: 2px solid #eabb66;
    outline-offset: 2px;
  }
</style>
